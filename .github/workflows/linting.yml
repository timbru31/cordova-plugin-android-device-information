on: [push, pull_request]

name: Linting

jobs:
  tsLint:
    name: Runs TSLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.3.5
      - uses: actions/setup-node@v2.4.1
      - run: |
          npm ci
          npm run lint
